// Policy: No Network Access
// Blocks programs that use MC_56 (SOCKET)

PC no_network {
    import "ast_inspect.pcd";

    fn check(mc_ids) {
        if (contains_monomer(mc_ids, 56) == 1) { return 1; }
        return 0;
    }

    OUTPUT 0;
}
